void main() {
  String wifi_Connection = "Connected";
  if (wifi_Connection == "Connected") {
    print("WiFi is connected.");
  } else if (wifi_Connection == "Disconnected") {
    print("WiFi is disconnected.");
  } else {
    print("WiFi is not connected.");
  }
}